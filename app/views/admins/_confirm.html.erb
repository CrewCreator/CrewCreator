<%= form_for @current_user, url: {action: "confirm"}, :method => :POST do |f| %>
  <%= f.hidden_field :params, params %>
  <%= f.label "Confirm Password" %>
  <%= f.password_field :password %>
  
  <%= f.submit "Remove #{@user_removing.name}" %>
<% end %>