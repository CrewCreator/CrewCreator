<div id='main'>
  <h1>All Courses</h1>
    <table id='courses'>
      <thead>
        <tr>
        <th>Code</th>
        <th>Course Name</th>
        <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <% @courses.each do |course| %>
          <tr>
            <td> <%= course.code %> </td>
            <td> <%= course.name %> </td>
            <td> <%= course.description %> </td>
            <td> <%= link_to 'Add Section', new_course_section_path(course), :id => "add_section_to_#{course}" %> </td>
            
          </tr>
          <tr>
            <% course.sections.each do |section| %>
              <td> <%= link_to section.number, course_section_projects_path(course, section) %> </td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= button_to 'Create Course', {:action => :new, :controller => :courses}, :method => :get %>
</div>