<%= form_for @current_user, url: {action: "destroy"}, :method => :POST do |f| %>
  <%= f.hidden_field :id, value: @object_removing.id %>
  <%= f.label "Confirm Password" %>
  <%= f.password_field :password %>
  
  <%= f.submit "Remove #{@object_removing.name}" %>
<% end %>