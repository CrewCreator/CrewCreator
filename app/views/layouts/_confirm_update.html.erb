<%= form_for object, url: {action: "update"}, :method => :POST do |f| %>
  <%= f.hidden_field :id, value: object.id %>
  <%= f.label "Confirm Password" %>
  <%= f.password_field :password, id: "admin_password" %>
  
  <%= f.submit "Update #{object.name}", id: "confirm" %>
<% end %>